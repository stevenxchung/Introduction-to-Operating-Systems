// The echo protocol client
// Pseudo code

// Include packages
#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <unistd.h>
#include <netdb.h>
#include <netinet/in.h>

// Constants
#define SERVER_NAME "localhost"
#define PORT 8000
#define MESSAGE "hello world!"

// Connect to server socket if available and send message
int main() {
    // Set localhost
    // Create socket
    // Connect to server if exists
    // Send message
    // Close socket
}
